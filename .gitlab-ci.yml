image: publysher/hugo

cache:
    paths:
    - /var/cache/apt/archives/

before_script:
  - apt-get install git-core

pages:
  script:
  - hugo
  artifacts:
    paths:
    - public
  only:
  - master
